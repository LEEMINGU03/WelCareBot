# WelCare 복지 정책 챗봇 — 기능명세서

**작성일:** 2026-05-10  
**프로젝트:** bockjiagent  
**기술 스택:** Python, CrewAI Flow, Gemini 2.5 Flash, SerperDevTool, ScrapeWebsiteTool

---

## 1. 시스템 개요

사용자의 상황(분야·지역·나이)을 대화로 파악하고, 실시간 웹 검색으로 맞춤 복지 정책 최대 5개를 추천한 뒤, 선택된 정책의 수혜 자격을 단계별 질문으로 확인해주는 AI 챗봇.

---

## 2. 아키텍처

```
사용자 입력
    │
    ▼
WelCareFlow (crewai.flow)
    │
    ├─ Phase: intake     → IntakeCrew      (intake_agent)
    ├─ Phase: search     → PolicySearchCrew (policy_researcher_agent)
    ├─ Phase: selection  → Flow 내부 로직 (_find_policy)
    ├─ Phase: eligibility→ EligibilityCrew (eligibility_agent)
    └─ Phase: done       → 결과 출력 후 상태 초기화
```

### 2.1 State (WelCareState)

| 필드 | 타입 | 설명 |
|---|---|---|
| `message` | str | 현재 사용자 입력 |
| `history` | List[str] | 전체 대화 이력 |
| `phase` | str | 현재 단계 (`intake` / `search` / `selection` / `eligibility` / `done`) |
| `conditions` | dict | 수집된 사용자 조건 (대분류, 소분류, 지역, 나이) |
| `policies` | List[dict] | 검색된 정책 목록 (최대 5개) |
| `selected_policy` | dict | 사용자가 선택한 정책 (URL 크롤링 상세 포함) |
| `result` | str | 봇 응답 텍스트 |

---

## 3. 페이즈별 기능 명세

### 3.1 Phase: intake — 사용자 정보 수집

**담당:** `IntakeCrew` → `intake_agent` + `intake_task`  
**LLM:** Gemini 2.5 Flash

#### 수집 흐름 (3단계 고정)

| 단계 | 수집 항목 | 방식 |
|---|---|---|
| STEP 1 | 대분류 선택 | 1~9번 목록 제시 / 번호·텍스트 모두 허용 |
| STEP 2 | 소분류 선택 | 대분류 내 소분류 목록 제시 / 없으면 자유입력 그대로 기록 |
| STEP 3 | 지역 + 나이 | 한 번에 질문, 둘 다 파악되면 즉시 완료 |

#### 복지 대분류 체계

| 번호 | 대분류 | 소분류 예시 |
|---|---|---|
| 1 | 주거 | 월세 지원, 전세자금 대출, 공공임대주택, 주거환경 개선 |
| 2 | 취업·일자리 | 구직활동 지원, 직업훈련, 창업 지원, 실업급여 |
| 3 | 의료·건강 | 의료비 지원, 건강검진, 정신건강, 장기요양 |
| 4 | 교육 | 장학금, 학비 지원, 직업교육, 평생교육 |
| 5 | 생계·소득 | 기초생활보장, 긴급복지, 근로장려금, 사회적 일자리 |
| 6 | 육아·가족 | 보육비 지원, 아동수당, 한부모 가족, 다자녀 지원 |
| 7 | 노인 | 노인 일자리, 요양 서비스, 노인 주거, 노인 의료 |
| 8 | 장애인 | 활동 지원, 장애인 일자리, 장애 수당, 장애인 교육 |
| 9 | 청년 | 주거 지원, 청년 저축, 취업 지원, 창업 지원 |

#### 출력 JSON 스펙

```json
{
  "status": "INCOMPLETE" | "COMPLETE",
  "follow_up_question": "다음 질문 (INCOMPLETE일 때만)",
  "conditions": {
    "welfare_type_major": "대분류",
    "welfare_type_minor": "소분류",
    "region": "거주 지역",
    "age": "나이"
  }
}
```

#### 상태 전이

- `COMPLETE` → `phase = "search"` (다음 페이즈 진행)
- `INCOMPLETE` → `state.result = follow_up_question` (같은 페이즈 유지)

---

### 3.2 Phase: search — 정책 검색

**담당:** `PolicySearchCrew` → `policy_researcher_agent` + `policy_search_task`  
**LLM:** Gemini 2.5 Flash  
**도구:** SerperDevTool (웹 검색 전용, 크롤링 없음)

#### 검색 방법

- 키워드: `{conditions} 복지 정책 신청`
- 우선 소스: bokjiro.go.kr, 고용노동부, 복지부, 지자체 사이트
- 검색 snippet에서 정보 추출 (추가 크롤링 없음)
- 최대 5개 정책 반환

#### 출력 JSON 스펙

```json
{
  "status": "SUCCESS",
  "policies": [
    {
      "name": "정책명",
      "benefit": "혜택 내용",
      "eligibility": "수혜 자격",
      "how_to_apply": "신청 방법",
      "deadline": "마감일",
      "url": "URL (없으면 빈 문자열)"
    }
  ]
}
```

#### 상태 전이

- 정책 1개 이상 → `phase = "selection"`, 목록 포맷 후 `state.result` 출력
- 정책 0개 → `phase = "intake"` 로 롤백, 안내 메시지 출력

#### 사용자 표시 포맷

```
아래 복지 정책을 찾았어요!
번호 또는 이름으로 더 알고 싶은 항목을 선택해주세요.

**1. 정책명**
   혜택 미리보기 (60자 + ...)

**2. 정책명**
   ...

예) `2` 또는 `청년 월세 지원`
```

---

### 3.3 Phase: selection — 정책 선택

**담당:** Flow 내부 로직 (`_find_policy` 함수)

#### 선택 매칭 우선순위

1. 숫자 직접 입력 (`"1"`, `"2"` 등)
2. 한국어 수 표현 (`"첫 번째"`, `"하나"`, `"1번"` 등)
3. 정책명 전체 포함 검색 (소문자 비교)
4. 단어 단위 부분 매칭 (2자 이상 단어)

#### 선택 성공 시 동작

1. `selected_policy` 설정
2. `phase = "eligibility"` 전환
3. 정책 URL 크롤링 시도 (`ScrapeWebsiteTool`, 최대 3000자)
4. `EligibilityCrew` 즉시 실행 → 첫 번째 자격 질문 포함한 응답 출력

#### 선택 실패 시 동작

- 안내 메시지 + 목록 재표시
- `phase` 유지 (`"selection"`)

---

### 3.4 Phase: eligibility — 자격 확인 대화

**담당:** `EligibilityCrew` → `eligibility_agent` + `eligibility_task`  
**LLM:** Gemini 2.5 Flash

#### 동작 원칙

- 대화 이력(`history`)을 참조하여 이미 확인된 조건은 재질문 금지
- 미확인 조건을 쉬운 말로 1개씩만 질문
- 정책 상세 정보(`selected_policy.detail`)에서 자격 조건 파악

#### 출력 JSON 스펙

```json
{
  "status": "ELIGIBLE" | "NOT_ELIGIBLE" | "INCOMPLETE",
  "message": "다음 질문 또는 확인 메시지",
  "reason": "판단 이유 (ELIGIBLE/NOT_ELIGIBLE일 때)"
}
```

#### 상태 전이 및 최종 응답

| status | 동작 | phase 전이 |
|---|---|---|
| `ELIGIBLE` | 신청 방법·마감일·URL 포함 최종 안내 출력 | `"done"` |
| `NOT_ELIGIBLE` | 사유 안내 + 다른 정책 목록 재표시 | `"selection"` |
| `INCOMPLETE` | 다음 질문 출력 | `"eligibility"` 유지 |

#### ELIGIBLE 최종 응답 포맷

```
✅ 신청 가능합니다!

**정책명**

**신청 방법:** ...
**마감일:** ...

[더 알아보기 →](URL)
```

---

### 3.5 Phase: done — 세션 초기화

- `phase == "done"` 확인 시 `state_data`와 `history`를 초기값으로 리셋
- 다음 대화는 `phase = "intake"` 부터 새로 시작

---

## 4. 보조 기능

### 4.1 TranslatorCrew (현재 미사용 / 예비)

**담당:** `translator_agent` + `translate_task`  
행정 용어를 쉬운 한국어로 순화 후 Markdown 형식으로 출력.  
예: `소득인정액` → `가정의 실제 수입으로 계산한 금액`

### 4.2 JSON 파싱 (`_parse_json`)

- LLM 출력에서 마크다운 코드펜스(` ```json `) 제거
- 중첩 `{}` 블록을 모두 추출 후 역순으로 파싱 시도 (LLM 출력의 마지막 블록 우선)
- 이중 중괄호 `{{ }}` → `{ }` 자동 변환

---

## 5. 외부 의존성

| 항목 | 용도 | 환경변수 |
|---|---|---|
| Gemini 2.5 Flash | 모든 Agent LLM | `GEMINI_API_KEY` |
| SerperDev | 복지 정책 웹 검색 | `SERPER_API_KEY` |
| ScrapeWebsiteTool | 선택 정책 URL 상세 크롤링 | (내장) |

---

## 6. 실행 방법

```bash
# CLI 직접 실행
python main.py

# 초기 메시지 전달
python main.py 청년 월세 지원 알고 싶어요
```

---

## 7. 전체 대화 흐름 다이어그램

```
사용자 접속
    │
    ▼
[intake] 대분류 선택 안내
    │  ← 대분류 선택
    ▼
[intake] 소분류 선택 안내
    │  ← 소분류 선택
    ▼
[intake] 지역·나이 질문
    │  ← 지역·나이 입력
    ▼
[search] SerperDev 웹 검색
    │
    ├─ 결과 없음 → [intake] 롤백
    │
    ▼
[selection] 정책 목록 표시 (최대 5개)
    │  ← 번호 또는 이름 선택
    ▼
URL 크롤링 (상세정보 보강)
    │
    ▼
[eligibility] 첫 번째 자격 조건 질문
    │  ← 사용자 답변
    ▼
[eligibility] 반복 질문
    │
    ├─ ELIGIBLE    → ✅ 신청 안내 → [done] → 세션 초기화
    └─ NOT_ELIGIBLE → 사유 안내 → [selection] 목록 재표시
```
